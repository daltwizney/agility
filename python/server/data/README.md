# server/data

Static media the server reads at runtime and serves to clients (UE5, browser, etc.).

Files here are bundled with the plugin and published to the public repo — only commit content that's small, license-clean, and meant to be shared. For local-only experimentation media, use a gitignored directory instead.
